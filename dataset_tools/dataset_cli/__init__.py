"""kubecore-dataset — a small CLI to log in to lakeFS, validate an
Ultralytics-pose dataset, and incrementally sync it (adds + deletes) to a
lakeFS branch, at the path the pipeline reads.
"""

__version__ = "1.1.0"
