# Dependencies to build the documentation site.
# Used by .github/workflows/docs.yml. Pinned so the build stays stable.
mkdocs-material>=9.5,<10
mkdocs-glightbox>=0.4,<1   # click-to-zoom for screenshots
